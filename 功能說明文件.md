# PDF 壓縮工具 — 功能說明文件

> 版本截止日期：2026-06-01

---

## 目錄

1. [支援格式](#支援格式)
2. [壓縮模式](#壓縮模式)
3. [壓縮選項](#壓縮選項)
4. [檔案管理](#檔案管理)
5. [輸出控制](#輸出控制)
6. [批次處理與進度](#批次處理與進度)
7. [圖片分析](#圖片分析)
8. [壓縮日誌](#壓縮日誌)
9. [介面與主題](#介面與主題)
10. [設定記憶](#設定記憶)
11. [技術架構](#技術架構)
12. [後續建議功能](#後續建議功能)

---

## 支援格式

| 格式 | 副檔名 | 說明 |
|------|--------|------|
| PDF | `.pdf` | 三種壓縮模式均支援，另支援「解除密碼」模式 |
| JPG | `.jpg` / `.jpeg` | 透過 Pillow 重採樣與 JPEG 重編碼 |
| PPTX | `.pptx` | 拆 ZIP 後壓縮 ppt/media/ 媒體圖片 |
| PPT（舊版）| `.ppt` | 不支援；顯示提示請先另存為 .pptx |

同一批次可混入多種格式，透過「壓縮格式」checkbox 可個別啟用或停用。

---

## 壓縮模式

以左側選項區的**分段按鈕**切換，選中項以青藍色高亮。

### 模式 0 — 🔓 解除密碼

- 開啟加密 PDF 並另存為**無密碼版本**，此模式僅去除密碼，**不進行壓縮**
- **純權限密碼**（禁止列印 / 複製 / 編輯）的檔案：自動移除限制，無需輸入密碼
- **開啟密碼**（開檔即需密碼）的檔案：逐一彈出遮罩式密碼輸入框，輸入正確密碼即可解除；密碼錯誤可重試最多 3 次，取消則該檔標記為「已略過」
- 密碼輸入框附**眼睛圖示**👁，可切換明碼 / 遮罩顯示
- 僅處理 PDF（忽略格式勾選），輸出檔名為「原檔名**_unlocked**.pdf」
- 為避免彈框衝突，此模式強制單執行緒依序處理
- ⚠️ 本功能需正確密碼，**無法破解**；請確認您擁有合法處理該檔案的權限

> 技術：以 `pikepdf.open(path, password=…)` 開啟後 `save()` 不帶 `encryption` 參數即輸出無密碼版本（`core/decrypt.py`）。

### 模式 1 — 無失真

- 使用 **pikepdf** 重壓 PDF stream，不修改任何圖片像素
- 不調整 DPI、不重編碼 JPEG，輸出畫質與原始完全一致
- 適合文字型 PDF 或已最佳化過的掃描檔

### 模式 2 — 圖片優化

- **僅**對超過目標 DPI 的圖片進行降採樣，不升採樣
- JPEG 重編碼品質固定為 95（高保真），差異幾乎難以察覺
- 降採樣後再套用無失真 flate 壓縮
- 適合高解析度掃描文件、相機直出的 PDF

### 模式 3 — 高壓縮

- **所有圖片**均以使用者指定品質重編碼（50–95 可調）
- 同樣對超過 DPI 上限的圖片降採樣
- 壓縮率最高，適合純分享、不需精準輸出的場景

> PDF 各模式的降採樣使用 pikepdf 讀取圖片 CTM 精確計算有效 DPI，而非以固定頁面尺寸估算。

---

## 壓縮選項

### 壓縮等級

- 滑桿範圍 1–9，但 pikepdf 9+ 自動選擇最佳等級，因此滑桿顯示為 disabled
- 保留 UI 供未來降級至 pikepdf 8 系列時使用

### 圖片解析度（降採樣上限）

下拉選單，選項如下：

| 選項 | 使用情境 |
|------|----------|
| 300 DPI（印刷） | 高品質印刷輸出 |
| 200 DPI（高品質） | 螢幕閱讀 + 偶爾列印 |
| 150 DPI（一般文件） | 預設值，日常文件 |
| 96 DPI（螢幕） | 純螢幕顯示，檔案最小 |
| 72 DPI（網頁） | 網頁嵌入，解析度最低 |

僅模式 2 / 3 有效。

### 圖片品質（JPEG 重編碼）

- 滑桿範圍 50–95
- 僅模式 3 有效（模式 2 固定 95）
- 值越低壓縮率越高，但圖片品質也越低

### 保留原始檔案建立/修改日期

- 勾選後透過 `os.utime()` 還原修改時間與存取時間
- 建立時間在 Windows 另以 `ctypes.windll.kernel32.SetFileTime` 還原
- 預設：勾選

### 保留原始檔名

- 勾選：輸出檔名與原始相同，存入 `compressed/` 子資料夾
- 取消勾選（預設）：輸出檔名加上 `_compressed` 後綴，存於相同或指定目錄

---

## 檔案管理

### 加入方式

- **拖放檔案**：將檔案拖入 DropZone 或視窗任意位置
- **拖放資料夾**：拖入資料夾後彈出確認對話框，顯示第一層可壓縮檔案數量；確認後僅加入第一層，不含子資料夾
- **點擊瀏覽**：點擊 DropZone 或「瀏覽檔案」按鈕開啟對話框

### 虛擬清單（高效能）

- 使用 `tk.Canvas` 自行管理捲動，僅渲染可視範圍 ±buffer 共約 10–20 筆
- 可順暢顯示數千個檔案，不影響 GUI 回應速度
- 載入 385 個檔案：SSD 30s→3s、網路磁碟 71s→6s

### 非同步載入

- 拖放 / 瀏覽後在背景執行緒讀取 metadata（`os.path.getsize`、`os.path.isdir`）
- 每 10 筆透過 `after(0, …)` 回主執行緒分批建立 widget
- DropZone 即時顯示「⏳ 載入中 N / Total …」

### 取消載入

- 載入中「全部清除」按鈕自動切換為紅色「取消載入」
- 點下立即中止 worker；已載入的檔案保留
- 可應對誤選大量檔案的情境

### 格式篩選

- 選項區底部的 **PDF / JPG / PPTX** checkbox 可個別控制
- 取消勾選某格式後，清單中不符格式的條目即時變暗，壓縮時自動略過

### 標題列資訊

- 已選檔案數量與總容量即時顯示（自動切換 MB / GB）

### 移除單檔

- 每個檔案條目右側有 ✕ 按鈕，點擊立即移除

### 全部清除

- 標題列「全部清除」按鈕一鍵清除所有已選檔案

---

## 輸出控制

- **輸出資料夾**：輸入框可直接鍵入或點擊「瀏覽」選取
- **空白**：與原始檔案相同目錄（預設）
- **指定目錄**：所有輸出集中至同一資料夾
- 搭配「保留原始檔名」可自動建立 `compressed/` 子資料夾

---

## 批次處理與進度

- 所有檔案在**背景 daemon 執行緒**依序壓縮
- 每個檔案完成後透過 `self.after(0, callback)` 安全回到 GUI 執行緒
- 進度列即時更新，右側青色百分比 label 顯示 0%→100%

### 停止壓縮

- 壓縮進行中，「開始壓縮」按鈕切換為紅色「⏹ 停止壓縮」
- 點下後設定 cancel flag，下一個檔案開始前中止
- 已完成的結果仍正常顯示

---

## 圖片分析

點擊「📊 分析圖片」按鈕後，在結果區顯示：

**PDF** — 使用 pikepdf 讀取每張圖片的 CTM（Coordinate Transformation Matrix），精確計算有效顯示 DPI，輸出：
- 圖片總數
- DPI 分佈統計（最小 / 最大 / 平均）
- 超過 300 DPI 的圖片數量

**JPG** — 讀取 EXIF 中的 DPI 資訊

**PPTX** — 統計 ppt/media/ 下：
- JPEG 圖片數量
- PNG 圖片數量
- 其他媒體數量
- 媒體總容量

---

## 壓縮日誌

### 即時結果

每個檔案完成後，結果區即時顯示：
- 輸出路徑
- 壓縮前後大小
- 壓縮率（百分比）
- 耗時（如 `2.3s`）
- 失敗原因（若有）

### 匯出日誌

壓縮完成後，標題列「📋 匯出日誌」按鈕啟用，點擊後：
- 開啟儲存對話框（預設檔名含時間戳）
- 輸出 UTF-8 `.txt`，內含：
  - 壓縮時間與設定快照
  - 每個檔案的詳細結果
  - 統計摘要（成功 / 失敗數、總節省空間、**總壓縮時間**）

---

## 介面與主題

- 深色 / 淺色 / 系統主題，右上角 🌙 按鈕一鍵切換
- 科技感 HUD 視覺：
  - **GradientDivider**：標題下方青色漸層分隔線
  - **AnimatedBorderFrame**：選項區滑鼠懸停邊框漸變為青色
  - **DropZone 拖入動畫**：邊框循環閃爍，提示文字改為「⬇ 放開以加入檔案」
- 可拖拉的雙窗格版面（**CTkSplitter**）：
  - 左側：DropZone、選項、輸出路徑、動作列
  - 右側上方：已選檔案清單
  - 右側下方：壓縮結果
  - 水平與垂直分割線皆可拖動，hover 時顯示青色

---

## 設定記憶

關閉視窗時自動寫入 `%APPDATA%\compress-pdf\settings.json`，下次啟動還原：

| 設定項 | 說明 |
|--------|------|
| mode | 壓縮模式（0 解除密碼 / 1 / 2 / 3） |
| dpi | 圖片解析度選項 |
| quality | 圖片品質（50–95） |
| preserve_dates | 是否保留原始日期 |
| keep_filename | 是否保留原始檔名 |
| output_dir | 輸出資料夾路徑 |
| theme | 介面主題 |
| fmt_pdf / fmt_jpg / fmt_pptx | 格式篩選狀態 |

---

## 技術架構

```
main.py                    # 入口：初始化 customtkinter，啟動 MainWindow
gui/
  main_window.py           # 主視窗：UI 建構、事件處理、批次壓縮流程
  widgets.py               # DropZone、FileListItem、AnimatedBorderFrame
                           # GradientDivider、CTkSplitter、VirtualFileList、PasswordDialog
core/
  compressor.py            # CompressOptions、CompressResult、compress_file/compress_batch
  decrypt.py               # pikepdf 解除 PDF 密碼（is_encrypted / decrypt_pdf）
  lossless.py              # pikepdf 無失真壓縮
  image_optimizer.py       # pikepdf + Pillow 圖片重採樣（降 DPI / JPEG 重編碼）
  jpg_compressor.py        # Pillow JPEG 三模式壓縮
  pptx_compressor.py       # zipfile + Pillow PPTX 媒體圖片壓縮
  _image_utils.py          # 共用：dpi_to_max_px、resample_by_max_px、resample_by_dpi
utils/
  file_utils.py            # 路徑生成、大小格式化、Windows SetFileTime 時間戳還原
  settings.py              # JSON 設定讀寫（%APPDATA%\compress-pdf\settings.json）
```

### 資料流

```
MainWindow → CompressOptions
           → compress_batch() ─── daemon thread
                                   └─ compress_file() ─┬─ decrypt_pdf()            (PDF 模式 0 解除密碼)
                                                        ├─ compress_lossless()      (PDF 模式 1)
                                                        ├─ optimize_images()        (PDF 模式 2/3)
                                                        ├─ compress_jpg()           (JPG)
                                                        └─ compress_pptx()          (PPTX)
                                   └─ after(0, on_progress) → GUI thread
```

### 依賴套件

| 套件 | 版本要求 | 用途 |
|------|----------|------|
| pikepdf | ≥ 8.0 | PDF stream 壓縮、圖片 CTM 計算 |
| pymupdf | ≥ 1.24 | 宣告於 requirements，目前保留備用 |
| customtkinter | ≥ 5.2 | 現代化 tkinter 主題與元件 |
| tkinterdnd2 | ≥ 0.3 | 拖放支援 |
| Pillow | — | 圖片重採樣（requirements 中未列，隨 pikepdf 自動安裝） |

---

## 後續建議功能

以下功能依優先度與實作難度評估排列。

### P1 — 高優先

#### 1. PNG / WebP / TIFF 圖片格式支援
- **動機**：PNG 無損但通常未壓縮，WebP 壓縮率更高；設計稿、截圖常用這三種格式
- **實作**：在 `core/` 新增 `png_compressor.py`，複用 `_image_utils.py` 的降採樣邏輯；WebP 可透過 Pillow `save(..., format="WEBP")` 輸出

#### 2. 預覽壓縮率（Dry-run 估算）
- **動機**：壓縮前讓使用者知道「大概能省多少」，協助選擇適當設定
- **實作**：對前幾頁採樣壓縮後估算整體比例，或顯示歷史壓縮率統計；需要額外後台執行緒，UI 以小 badge 呈現

#### 3. 進度通知（系統通知）
- **動機**：壓縮大量檔案時視窗可能被遮住；完成後應有系統層級提示
- **實作**：Windows 可用 `plyer` 或 `win10toast` 推送通知；需新增「完成後通知」checkbox

---

### P2 — 中優先

#### 5. 比較預覽（Before / After 視覺對比）
- **動機**：讓使用者直接看到壓縮對圖片品質的影響，特別是模式 3
- **實作**：使用 pymupdf（已在 requirements）渲染第一頁為圖片，左右並排顯示；可在結果列表的「預覽」按鈕觸發

#### 6. 壓縮 Preset（預設設定組合）
- **動機**：常見使用場景固定（如「上傳用」「存檔用」），不需每次手動調整
- **實作**：在選項區頂部加入下拉「套用 Preset」，儲存至 `settings.json` 的 `presets` 陣列；支援自訂命名

#### 7. 拖放重排檔案順序
- **動機**：控制批次輸出的處理順序，特別是與「檔案序號」等功能搭配
- **實作**：`VirtualFileList` 加入 drag-to-reorder 邏輯；需處理虛擬清單與實際 `_entries` 的索引同步

---

### P3 — 長期 / 可選

#### 8. PDF 頁面管理（分割 / 合併）
- **動機**：常見的 PDF 工作流程，與壓縮功能互補
- **實作**：新增「工具」標籤頁，分割以 `pikepdf.Pdf.save` 按頁範圍輸出，合併以 `pikepdf.Pdf` 拼接 pages

#### 9. PDF 加密 / 解密
- **解密**：✅ 已完成（模式 0 解除密碼，見上方「壓縮模式」）
- **加密**（待辦）：分享壓縮後的 PDF 時設定開啟 / 權限密碼
- **實作**：`pikepdf` 原生支援 `encryption` 參數；在輸出選項區加入密碼輸入框（可選）

#### 10. 壓縮歷史紀錄
- **動機**：追蹤哪些檔案在何時用什麼設定壓縮過
- **實作**：每次壓縮結果 append 至 `%APPDATA%\compress-pdf\history.json`；新增「歷史」側邊欄，支援搜尋與重新壓縮

#### 11. 命令列介面（CLI）
- **動機**：自動化腳本、CI 管線整合
- **實作**：以 `argparse` 包裝 `compress_batch()`；`main.py` 判斷 `sys.argv` 決定 CLI 或 GUI 模式

#### 12. 雲端儲存整合（Google Drive / OneDrive）
- **動機**：直接從雲端拿檔、壓縮後上傳回去
- **實作**：使用各家 SDK；需處理認證流程，建議以外掛 / 選用依賴方式加入，不影響基本使用

#### 13. 跨平台支援（macOS / Linux）
- **動機**：目前時間戳還原使用 Windows API，其餘功能理論上跨平台
- **實作**：`utils/file_utils.py` 的 `_set_creation_time_windows` 已有平台判斷；macOS 可用 `SetFile -d`（通過 subprocess），Linux 無法設定建立時間（顯示 N/A 即可）
